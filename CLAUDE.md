# CLAUDE.md

이 repo에서 작업할 때 참고하는 현재 상태와 규칙.
"왜 이렇게 결정했는가"는 `decisions/`, 재현 로직은 `scripts/pipeline.py` 참고.
이 파일은 최신 상태로 덮어써서 갱신한다 (append-only 아님).

## 프로젝트

- 이름: Failure Root-Cause Analysis (FRA) / 고장 원인 분석
- TYM 중앙기술연구소, 데이터 분석가 1인 (VS Code + Claude Code로 전환, 2026-09-07)
- 목적: 클레임 데이터로 고장 원인을 규명해 **협력사 / 엔진사 / 자사** 중 누가 무엇을 해야 하는지 결론 도출
- 1차 대상: **전차축(Front Axle), 북미**

## 사용자 배경 (작업 스타일 참고)

- 데이터 분석가 2년차, 개발자 출신, 충남대 데이터사이언스 석사 과정 중
- **성장 모드 선호**: 답을 바로 주기보다 질문으로 유도, 판단 근거를 언어화하게 유도
- 통계 개념은 트랙터 클레임 맥락의 구체적 예시로 설명하면 잘 이해함
- **모든 것을 Claude에게 맡기지 않고 직접 코드를 보고 실행하며 검증하고 싶어함**
  (2026-09-07, VS Code+Claude Code 전환 계기)
- 막히면: 여기까지 한 것 / 정확히 막힌 지점 / 가설(틀려도 됨) 순으로 정리 후 힌트 요청

## 배경 (지난 실패)

2024년 국내·북미 분석 2건이 팀장·소장·상품기획팀까지 보고됐으나 조치로 이어지지 못함.
원인: 전처리 설명이 분량 대부분 차지 / 결과가 marginal에 머물러 조치 주체 특정 안 됨.

## TYM 도메인 구조

TYM은 조립(assembly) 회사. 부품 검사는 협력사가 완료 후 납품.
엔진/트랜스미션은 이원 구조 — ① 외부 완제품 구입(Deutz 등) ② 자사 설계·제작.

귀책 3갈래: ① 협력사 조달 부품 결함 ② 외부 엔진사 귀책 ③ 자사 설계·조립 귀책

⚠️ 귀책을 직접 알려주는 컬럼은 클레임 데이터에 없음. 부품번호-공급사 매핑표(별도 확보,
아래 참고)로 사후 매핑 시도 중이나 매칭률 67.6%에 그침.

## 분석 원칙 (반복 검증으로 도출됨)

1. **marginal 아니라 conditional** — 변수를 뭉갠 집계에서는 조치가 나오지 않음
2. **분모 명시** — 고장률 분모(생산·판매 대수) 미확보, 건수 비교만 가능
3. **표본 크기 항상 명시** — 쪼갤수록 n 급감. 카이제곱 등 검정 전 필수
4. **결론부터 쓰기** — 전처리는 부록
5. **외부 교차검증** — Reddit/TractorByNet 등 독립 소스와 경향 일치 확인
6. **Status ≠ 고장 유무**, **ClaimType__c 이름도 곧이곧대로 믿지 말 것**
   — 'Damaged'/'In House'는 이름과 달리 대다수 실제 고장이었음(실측 확인 필요)
7. **컬럼명·값 이름을 절대 믿지 말고 `DescriptionOfFailure__c` 텍스트로 대조 후 판단**
   — 이 원칙을 어겨서 분자 정의가 4번 정정됨(아래 이력 참고)
8. **부품 리비전(끝자리 숫자/알파벳)은 "같은 부품의 버전"이 아니라 "호환 안 될 때
   파생되는 별개 부품"** — prefix 매칭(앞자리만 비교)으로 뭉치면 안 됨. 정확 매칭만 사용.
9. **참조 매핑 파일이 불완전할 수 있음** — `2025_통합기종명.xlsx`는 T264/T754/T654 등
   다수 기종 누락 확인됨. 대신 `웹사이트_스펙_검토_260629.xlsx`의 "Group" 정보 사용.

## 데이터 구조 핵심

### 북미는 2단계 청구 구조 (가장 중요)
```
딜러 → TYM-USA (North America 레코드) → 본사 (Global 레코드, 처리 안되면 재클레임)
```
연결키: Global의 `OriginalClaimNumber__c` = NA의 `Id`(15자리 정규화). Global이 최종 판정.

### 분자(고장) 정의 v4 — 최종 확정 (4단계 재검증 거침)
```python
cond = (
    (master['ClaimType__c'] != 'Shortage')
    & (master['CauseCode__c'].notna())
    & (master['CauseCode__c'] != 'Shipping Damage')
    & (master['CauseCode3__c'].isna()
       | (~master['CauseCode3__c'].isin(['Product improvement campaign', 'Steering'])))
)
```
- `In Stock`, `In House` 필터 **없음** — 이름과 달리 실제 대다수 진짜 고장이었음
- 상세 정정 이력: `scripts/pipeline.py` STEP 2 주석 참고 (반드시 읽을 것)

## 확정 수치 (2026-09-07 기준)

```
원본 고유 클레임 57,901 (Korea 32,610 / NA 13,482 / Global 11,809)
북미 마스터      13,482 (fra_master_v3.csv)
Front Axle (분자정의 v4 적용)  1,991건
  └ Branson(단종 브랜드) 133건 제외 →  **최종 분석 대상 1,858건**

⚠️ 숫자 인용 시 기준을 반드시 명시할 것 (아래 두 기준이 혼용되기 쉬움):

| 카테고리 | 1,991건 기준(Branson 포함) | **1,858건 기준(Branson 제외, 실제 분석용)** |
|---|---|---|
| No Part Info | 562 | **540** |
| Seal | 454 | **427** |
| Bearing | 394 | **353** |
| Gear | 198 | **177** |
| Washer | 116 | **116** |
| Cylinder | 34 | **31** |
| Ring(Snap/C/O) | 34 | **30** |
| Circlip/Snap Ring | 8 | **7** |

→ 차이 합계 133 = Branson 건수와 정확히 일치 확인됨(검증 완료).
→ **분석·보고 시에는 1,858건 기준 숫자를 사용한다.**

플랫폼 매칭(웹사이트 스펙 Group 기준): 1,661/1,858 (89.4%)

주요 발견: 플랫폼별 부품 프로파일이 다름 (표본 20건+ 플랫폼 기준)
  Group/Model 4, 5 (소형기): Seal+Bearing 94~98%, Gear 거의 없음(1.5~4.8%)
  Group/Model 14 (T754 등): Gear 62.7% 압도적
  Group/Model 10 (T494/T574, 핵심 대상, n=433): Seal/Bearing/Gear/Washer 고르게 분산
  → 카이제곱 등 통계 검정으로 유의성 확인 필요 (다음 작업)
```

## 부품-공급사 매핑 (파일: `부품코드-공급사_매핑.xlsx`, 옥천/익산 기준)

- **정확 매칭(exact)만 사용.** 매칭률 67.6% (891/1,318건)
- ⚠️ prefix 매칭(앞 9자리 등) 시도했으나 폐기 — 리비전 다른 부품을 억지로 묶어
  97.8%까지 나왔지만 이는 착시. 구매팀 확인: 부품코드 끝자리(80→81→82)와
  접미 알파벳(A/B/C)은 "호환 안 될 때 파생되는 별개 부품"이라 정확 매칭만 유효.
- 복수 승인 공급사 부품 15개 확인됨(베어링 계열 다수) — 정상 현상(복수 벤더 운용)
- ISEKI & CO.,LTD.는 실제 부품 납품 협력사 맞음 (확인됨)
- **파킹 중**: 매칭 안 된 87개 부품(427건)이 특정 플랫폼(T264, T494/T574)에 집중되는
  이유 미확인. 시기와는 무관함(확인됨). 사용자가 추가 확인 중.

## 알려진 한계

1. 고장률 분모 없음 2. 귀책 매핑 불완전(67.6%) 3. 보증 만료일 컬럼 없음
4. 유상 수리 데이터 없음(마모성 고장 과소관측) 5. 관측기간 편중(생산11년 vs 고장기록3년)
6. **부품정보 없는 562건(No Part Info)이 Breakage에 편중** — 부품단위 결론에 명시 필수
7. 시리얼번호 결측 98.7% 8. `IsSentHQ__c` 불일치 2,667건(SFDC 도입 과도기, 추적불가)

## 현재 상태 / 다음 (2026-09-07)

- ✅ 마스터·분자정의·카테고리·플랫폼그룹·공급사매칭(부분) 완료
- ✅ Claude Code로 작업 환경 전환 — 사용자가 코드를 직접 읽고 실행하며 검증 참여
- ⏭ **다음: 플랫폼별 부품 프로파일 차이의 통계적 유의성 검정** (카이제곱 등)
  - Group/Model 14의 Gear 62.7% vs 다른 그룹 평균이 유의미한 차이인지
  - 검정 전 표본 크기 재확인 필수 (n=51인 그룹 등 작은 표본 주의)
- 이후: 귀책 가설 정리(부품-공급사 매칭 가능한 범위 내), 보고서 초안
  (템플릿: 결론→근거→귀책→제안액션→한계)

## 데이터 무결성 검증 결과 (2026-09-07, Claude Code 전환 시점 전수 검증)

3개 파일 모두 검증 통과. 아래는 Claude Code에서 분석 시작 시 참고할 기준값·결측률.

```
[정합성]
fra_master_v3        13,482행 / 58컬럼 / final_id 고유 13,482 (중복 0)
                     record_source: Global 8,788 + NA_standalone 4,694
분자정의 v4 적용      13,213건 → Front Axle 1,991건
fa_with_platform     1,858행 / 62컬럼 / 중복 0 / Branson 잔존 0건
fa_vendor_exact      1,318행(부품정보 있는 건만) / 매칭 891건(67.6%)
                     n_vendors 분포: 1개 832 / 2개 50 / 3개 9
파일 간 참조 정합성    fa→master, vendor→fa 모두 100% 존재 확인

[핵심 컬럼 결측률 - 1,858건 기준]
CauseCode2__c              0.0%   (고장현상, 완전)
UsageTime__c               0.0%   (사용시간, 완전)
AssetId                    0.0%   (재발 추적용, 완전)
fm_TotalApprovedAmount__c  0.0%   (승인금액, 완전)
fm_ItemCode__c             0.1%
fm_Glo_Kor_ProdDate__c     2.9%   (생산일자, 코호트 분석용)
total_approved_labor_hour  3.8%   (공임시간, 심각도 proxy)
platform_group            10.6%   (197건 미매칭)
causal_parts_no           29.1%   (540건, ⚠️ Breakage 편중 — 결론에 한계 명시 필수)
```

**통계 검정 시 주의**: 표본 20건 미만 플랫폼이 8개(총 45건) 존재. 검정 대상에서
제외하거나 "표본 부족"으로 명시해야 함. 20건 이상은 15개 플랫폼.

## 작업 공간 역할 분담 (2026-09-07 확정)

| 공간 | 용도 | 하지 않는 것 |
|---|---|---|
| **Claude Code (VS Code)** | 코드 작성·실행, 통계 검정, 데이터 검증, 파이프라인 수정. 사용자가 코드를 직접 읽고 실행하며 참여 | 도메인 지식 추측(사내 확인 필요한 것은 FRA/Field로) |
| **FRA / Field** (claude.ai) | 도메인 지식 확인·해석, 담당자·팀장 답변 반영 판단, 분석 방향 재검토, 결과 해석, 보고서 문안 | 코드 실행(Claude Code로) |
| **FRA / Method** (claude.ai) | 통계·인과추론 개념 학습(카이제곱, Weibull, 코호트, 좌절단 등). 실전에서 막힌 개념을 여기서 깊게 | 실제 데이터 분석 |
| **FRA / SQL** (claude.ai) | SQL 문법·쿼리 사고력 훈련. 실전 사례를 소재로 연습 | 실전 결론 도출 |

**원칙**: 결과 해석·판단은 FRA/Field, 실행·검증은 Claude Code. 개념이 막히면 FRA/Method.
Claude Code에서 나온 수치를 FRA/Field로 가져와 "이게 무슨 의미인지" 해석하는 흐름을 권장.

## 데이터 파일 위치 (이 handoff 패키지 기준)

```
data/fra_master_v3.csv                  전체 마스터 (13,482 x 58)
data/fra_front_axle_with_platform.csv   Front Axle + 카테고리 + 플랫폼그룹 (1,858)
data/fra_front_axle_vendor_exact.csv    위 + 공급사 매칭(exact only)
scripts/pipeline.py                     전체 재현 로직 (문서 겸용)
```

원본 raw 파일(SFDC 클레임 전체, 부품-공급사 매핑 엑셀 등)은 용량·보안상 이 패키지에
미포함. 필요 시 사용자가 별도 제공.

## 코딩 규칙

- Python(pandas). 분모·조건화 기준을 주석에 명시
- **모든 필터 조건은 적용 전 `DescriptionOfFailure__c` 텍스트로 샘플 대조 후 확정**
  (이름만 보고 필터링해서 반복적으로 오류 발생했던 이력 있음, 원칙 7 참고)
- 통계 검정 시 표본 크기(n)를 항상 결과와 함께 명시
- 사용자가 직접 실행하고 검증할 수 있도록, 결과만 요약하지 말고 재현 가능한 코드로 제시
