# CLAUDE.md

이 repo에서 작업할 때 참고하는 현재 상태와 규칙.
"왜 이렇게 결정했는가"는 `decisions/`, 필드 상세는 `notes/field-definitions.md` 참고.
이 파일은 최신 상태로 덮어써서 갱신한다 (append-only 아님).

## 프로젝트

- 이름: Failure Root-Cause Analysis (FRA) / 고장 원인 분석
- TYM 중앙기술연구소, 데이터 분석가 1인
- 목적: 클레임 데이터로 고장 원인을 규명해 **협력사 / 엔진사 / 자사** 중 누가 무엇을 해야 하는지 결론 도출
- 1차 대상: **전차축(Front Axle), 북미**

## 배경 (지난 실패)

2024년 국내·북미 분석 2건이 팀장·소장·상품기획팀까지 보고됐으나 조치로 이어지지 못함.
원인: 전처리 설명이 분량 대부분 차지 / 결과가 marginal에 머물러 조치 주체 특정 안 됨 /
북미 보고서 표지에 "정보전달" 체크(애초에 의사결정 요청이 아님).
상세: `notes/past-analysis-review.md`

## TYM 도메인 구조

TYM은 조립(assembly) 회사. 부품 검사는 협력사가 완료 후 납품.
엔진/트랜스미션은 이원 구조 — ① 외부 완제품 구입(Deutz 등) ② 자사 설계·제작.

귀책 3갈래를 전제로 분석 설계:
- ① 협력사 조달 부품 결함 → 협력사 클레임
- ② 외부 엔진사 귀책 → 엔진사 대응
- ③ 자사 설계·조립 귀책 → 설계변경/공정개선

추가 확인된 넷째 범주: **"고장은 실재하나 회사가 비용을 부담하지 않는 것"**
(보증만료·사용자과실 등, Status=Rejected). 귀책 구분과 별개로 분자 필터링 시 고려.

⚠️ **귀책을 직접 알려주는 컬럼은 데이터에 없음.** CauseCode + 부품번호로 사후 매핑 필요.

## 분석 원칙

1. **marginal 아니라 conditional** — 변수를 뭉갠 집계에서는 조치가 나오지 않음
2. **분모 명시** — 생산대수 vs 가동시간(machine-hours), left-truncation 주의
3. **표본 크기 확인** — 쪼갤수록 n 급감. 과거 n=13으로 "경향" 주장한 사례 있음
4. **결론부터 쓰기** — 전처리는 부록
5. **외부 교차검증** — Reddit/TractorByNet 등 독립 소스와 경향 일치 확인
6. **Status ≠ 고장 유무** — 승인/거절은 비용 판정
7. **자동생성 문서는 원본 대조 후 사용** — Copilot 카탈로그 등
8. **컬럼명을 믿지 말고 실측 채움률로 판단** — `Kor_`가 붙어도 공통일 수 있고, 이름과 실제 의미가
   다른 사례 다수 확인됨 (`ResponsibilityID__c`, `TM_Code__c`, `fm_startMonth__c` 등)

## 데이터 구조 — 핵심 3가지

### ① 3개 지역, 사실상 3개 스키마

`RecordType.Name` = Korea / North America / Global

| | 한국 | 북미+글로벌 |
|---|---|---|
| 고장 원인 | 자유 텍스트 (`Kor_Causes__c`, 고유값 5,000+) | 정형 코드 (`CauseCode__c` 29종) |
| 고장일자·사용시간 | 41% | 100% |

### ② 한국 B/S는 고장이 아님

`Kor_RequestTypeA__c` = A/S(고장 신고) / B/S(캠페인·정기서비스) / PDI(출고전 점검)
- 전체 UsageTime 66.6% 결측의 원인 = B/S 18,640건
- B/S = 품질본부가 문제 기대 리스트 전달 → 자동 무상수리 생성. 고장 안 났는데 클레임으로 기록
- 고장 집계 시 A/S만 사용. B/S는 "과거 조치 이력"으로 별도 활용 가치 있음

### ③ 북미는 2단계 청구 구조 (가장 중요)

```
딜러 → TYM-USA (North America 레코드)  →  본사 (Global 레코드)
       거의 항상 Closed                     여기서만 Rejected 발생
```

- 연결키: Global의 `OriginalClaimNumber__c` = NA의 `Id` (8,399건 100% 매칭)
- NA에서 시스템상 처리가 안 돼서 Global로 재클레임하는 구조. **완전한 데이터는 Global 쪽**
- `NA=Rejected & Global=Closed` = 0건 → 거절은 본사 단방향
- **Rejected의 의미**: NA 담당자가 승인해 넘긴 건을 본사 글로벌 서비스팀이 최종 거절. 고장은 실재함
- ⚠️ **CaseNumber 중복은 0건이지만 1클레임 ≠ 1고장사건.** 중복 제거 없이 집계하면 1.6배 부풀려짐

## 확정 모집단 (2026-08-27 v2 갱신)

```
북미 고유 고장사건 = 12,834건  (v1의 12,856건에서 NA-Global 22건 중복 추가 제거)
  ├─ NA 단독 (재클레임 안 됨: Rejected 또는 직접 처리)   4,457
  └─ NA→Global 재클레임분 (Global 레코드 사용)          8,377  (고유 NA 기준, 8,399는 Global 행 수)

제외: Global 단독 2,822건 = 비북미 (BAD BOY, MCCORMICK, RURAL KING 등 해외 파트너사)
      한국 31,899건 = 2차 과제

Front Axle: 1,970건  (v1 2,024 → 1,972 → 1,970으로 정밀화)
  → 1,972에는 NA-Global 22건 중복 중 Front Axle 2건이 아직 남아있었음(v2에서 해소)
  Leakage / Breakage가 약 90% 차지. 기준을 여러 번 바꿔도 이 비율 자체는 안정적
```

작업 테이블(v2): `fra_claim_event`(LEFT JOIN, 12,856행 — 22건 중복 포함) →
`fra_claim_event_dedup`(12,834건, 중복 제거 완료) → `fra_failure_only`(뷰, 분자 정의 적용)
생성 로직: `scripts/build_master_table_v2.sql`

⚠️ v1 테이블(`fra_na_base`, `fra_master`, UNION ALL 방식)은 NA 전용 컬럼
(ResponsibilityID__c, TM_Code__c 등)이 소실되는 결함이 있어 v2로 대체됨.
새 코드는 v2를 사용할 것.

## 분자(고장) 정의 — 확정

- **Status는 그대로 신뢰한다.** Global이 최종 판정 주체이며, AdminNotes에 "APPROVED"가 있어도
  그건 NA의 1단계 판단 기록일 뿐 재분류 불필요
- **In Stock 제외** — `ClaimType__c='In Stock'`은 판매 전 상태 (UsageTime 중앙값 1.4h,
  고장일<판매일 76.7% + 판매일자 없음 14.8% = 91.5%). 단 "출고 품질" 별도 지표로는 활용 가치 있음
- **Damaged / Shortage / In House 제외** — 물류·행정 이슈
- **Rejected는 분자에 포함** — 고장은 실재하고 비용 판정만 거절된 것

## 추가 확인 사항 (2026-08-27, 임원진/담당자 인터뷰)

### B/S 캠페인의 정체 — 선제적 리콜

품질본부 확인: 캠페인 대상 기대는 **설계 또는 생산·조립 단계에서 결함이 확인된 기대**.
고장 발생 여부와 무관하게 선제적으로 수리한다 (자동차의 리콜과 유사한 개념).
→ **캠페인 이력이 "회사가 이미 설계/조립 문제로 판정한 것"의 정답지일 수 있음.**
캠페인 사유에 설계/조립 구분이 기록되어 있는지, 대상 리스트(기대+사유) 접근 가능한지 확인 필요.
→ Notion "귀책 구분 반영한 분석 설계 짜기" 항목과 연결.

### 설계변경 적용 시점 — 코호트 분석의 기준점 확보

담당자 확인: 설계변경(ECN) 승인·제출 후 **약 1개월부터 신규 생산분에 완전 적용**된 것으로 간주.
개별 기대 단위 추적은 어렵지만, 생산일자 기준 전/후 코호트를 나눌 수 있는 근사 기준을 얻음.

```
생산일자 < 설계변경일 + 1개월  → 구형 집단
생산일자 ≥ 설계변경일 + 1개월  → 신형 집단
```

FRACAS의 Verify 단계(설계변경 효과 검증)에 사용 가능. 단, before/after 단순 비교의 함정
(대조군 없음, 계절성 등 혼입 변수) 주의 — 원칙 3, 스터디방(FRA/Method) 1-B/2-A 참고.

### ⚠️ 유상 수리 데이터 누락 가능성 — 다음 세션 최우선 확인

임원진 피드백: 회사의 기존 분석 관행이 **무상(워런티) 건에만 국한**되어 있었음
(내구성 문제를 포인트로 삼았기 때문). 임원진이 이미 "유상 수리 건도 봐야 한다"고 지적한 바 있음.

**확인 필요**: `Warrnaty_Claim_Status`(파일명부터 Warranty) 안에 유상 수리 건이 실제로
포함되어 있는지, 아니면 별도 시스템(일반 정비/매출 시스템)에 있는지. 포함 안 되어 있다면
**우리 분석도 임원진이 이미 지적한 한계를 그대로 반복하게 됨.** `ClaimType__c` 값들
(Parts Warranty, Loader/Backhoe Warranty 등)도 전부 "Warranty" 명칭이라 유상 건 부재 가능성.

## 알려진 한계

- **보증 만료일 컬럼 없음** → "OUT OF WARRANTY" 거절을 데이터로 검증 불가. 기종별 보증정책 표 필요
- **귀책 구분 컬럼 없음** → 부품번호 + 협력사 매핑 필요
- **고장률 분모 없음** → 기종별 생산·판매 대수 필요 (기획팀 보유)
- 엔진 계열: 엔진번호 매핑 부재(엔진개발팀 요청 시 확보 가능) + 조직 민감도 → 2차 과제
- 클레임은 "보고된 것"만 → 미보고 고장 selection bias
- 거절 사유를 담은 정형 컬럼 없음 → `AdminNotes__c` 텍스트가 유일한 단서

## 현재 상태 / 다음

- ✅ 모집단 확정, 43개 컬럼 선별, `fra_master` 구축, 분자 정의 확정
- 🔄 **SFDC 담당자로부터 신규 데이터 수신 대기**
  - Parts(부품번호) — 귀책 구분의 열쇠
  - Causal Code (True=주원인 1개 / False=부원인 N개) → **True만 사용**
  - Labor Code
  - 어제자까지 최신 데이터 포함
- ⏭ 수신 후: 구조 파악(1:N 여부, 조인키) → 모집단 재확인 → 부품 단위 conditional 분석

수신 시 확인할 것: ①별도 테이블 여부·조인키 ②부품→협력사 매핑 마스터 유무
③Causal=True 클레임당 1개 보장 여부 ④코드 정의서 ⑤부품 라인별 승인 여부

## 운영 구조

- `FRA / Field` (Claude): 실전 분석, 의사결정
- `FRA / Method` (Claude): 방법론·통계 스터디 (현재 보류)
- `FRA / SQL` (Claude): SQL 훈련
- 이 repo: 코드·의사결정 기록
- Notion: [FRA 핵심 정리(통합)] + [SFDC 필드 정의서]

## 데이터 취급 규칙

- 클레임 원본(xlsx/csv)은 절대 커밋하지 않는다 (.gitignore)
- 협력사명·모델명 등 식별 정보는 예시에서 익명화
- 개인정보 컬럼(Contact*, EndCustomer*) 은 마스터 테이블에서 제외됨

## 코딩 규칙

- Python(pandas) / SQLite. 분모·조건화 기준을 주석에 명시
- SQLite에서 `NOT EXISTS` 서브쿼리 사용 시 **인덱스 필수** (없으면 전수반복으로 10분+ 소요)
- 통계 검정 시 표본 크기와 가정 위반 가능성을 함께 기록
