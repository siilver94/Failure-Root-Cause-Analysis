# CLAUDE.md

이 파일은 이 repo에서 작업할 때(Claude Code 포함) 참고해야 하는 현재 상태와 규칙을 담는다.
"왜 이렇게 결정했는가"는 여기 쓰지 않는다 → `decisions/`를 참고할 것.
이 파일은 최신 상태로 계속 덮어써서 갱신한다 (append-only 아님).

## 프로젝트 개요

- 이름: Failure Root-Cause Analysis (FRA) / 고장 원인 분석
- 소속: TYM 중앙기술연구소, 데이터 분석가 1인 프로젝트
- 목적: 트랙터 클레임 데이터를 기반으로 고장 원인을 규명하여, 협력사 부품 / 외부 엔진사 / 자사 설계·조립 중
  어디에 개선이 필요한지 실행 가능한(actionable) 결론을 도출한다.

## 배경 (요약)

기존 국내/북미 클레임 분석(2024)은 팀장, 소장, 상품기획팀까지 보고되었으나 조직적 조치로 이어지지 못했다.
자체 진단 결과, 두 분석 모두 전처리 과정 설명에 분량이 집중되어 있었고, 결과가 marginal distribution(변수를
뭉갠 집계)에 머물러 "누가 무엇을 해야 하는가"가 특정되지 않았다. 상세 진단은 `notes/past-analysis-review.md` 참고.

## TYM 도메인 구조 (분석 설계 시 항상 전제할 것)

TYM은 조립(assembly) 회사다. 부품 검사·품질검사는 외부 협력사가 완료하고, TYM은 조립을 담당한다.
엔진/트랜스미션은 이원 구조:
1. 외부 완제품 엔진 구입 (예: Deutz) — 귀책은 엔진사
2. TYM 자체 설계·제작 엔진/트랜스미션 (엔진개발팀·트랜스미션팀 담당) — 귀책은 자사 설계/조립

따라서 고장 원인 분석은 항상 아래 3갈래 귀책 구분을 전제로 설계한다:
- ① 외부 협력사 조달 부품 결함 → 협력사 클레임
- ② 외부 완제품 엔진사 귀책 → 엔진사 대응
- ③ TYM 자체 설계·조립 공정 귀책 → 설계변경/공정개선

이 구분이 없는 집계(marginal)는 실무적으로 반쪽짜리 결론이다.

추가로 실무 처리 과정에서 넷째 범주가 확인됨: **"고장은 실재하나 회사가 비용을 책임지지 않는 것"**
(보증기간 만료, 사용자 과실 등, SFDC Status=Rejected로 기록됨). 이는 귀책 구분과는 별개로,
"고장이 실재했는가"를 판단하는 필터링 단계에서 반드시 고려해야 한다.

## 핵심 원칙 (계속 갱신)

1. **Marginal이 아니라 conditional로**: 부위/기종/사용시간 등을 뭉갠 집계에서는 조치가 나오지 않는다.
   반드시 다른 변수로 조건화(condition)한 결과에서 결론을 낸다.
2. **분모 설계를 항상 명시**: 고장률의 분모가 생산 대수인지 누적 가동시간(machine-hours)인지에 따라
   결론이 달라진다. 특히 최근 생산분은 아직 고장 날 시간이 부족했을 뿐인 left-truncation 문제를 주의.
3. **표본 크기를 항상 확인**: 셀을 쪼갤수록(부위 × 시간대 × 기종 등) 셀 당 건수가 급격히 줄어든다.
   "N%"라는 숫자를 보고할 때 항상 실제 건수(n)를 함께 확인한다.
4. **보고서는 결론에서 시작**: 전처리 과정 설명은 부록으로. 첫 페이지에 "무엇을 바꿔야 하는가"가 있어야 한다.
5. **내부 결론은 외부 소스로 교차검증**: 내부 클레임 데이터만으로 낸 결론보다, 외부 커뮤니티(Reddit,
   TractorByNet 등) 같은 독립 소스와 경향이 일치함을 보여주는 결론이 훨씬 반박하기 어렵다.
6. **"승인/거절(Status)"과 "고장 실재 여부"는 다른 질문**: Rejected(거절)된 클레임 중 다수가 실제로는
   진짜 고장이 확인된 건이다 (승인 금액 0원 ≠ 고장이 없었다는 뜻). 분자를 정의할 때 이 둘을 혼동하지 않는다.
7. **자동 생성된 문서(Copilot 카탈로그 등)는 반드시 원본 대조 검증 후 사용**: 검증 방법은
   `notes/data-catalog-verification-2026-08-12.md` 참고.

## 데이터 구조 핵심 사실

- `Warrnaty_Claim_Status.xlsx` (SFDC 단일본, 55,954행 × 285열)는 **RecordType.Name**
  (Korea/North America/Global) 기준으로 사실상 3개의 다른 스키마가 공존하는 테이블이다.
  컬럼명이 `Kor_`로 시작한다고 반드시 한국 전용은 아니므로(예: `Kor_Migration__c`는 3지역 공통),
  실제 채움률로 판단해야 한다.
- 한국: 고장 원인이 자유 텍스트(`Kor_Causes__c`, 고유값 5,000+)이며, `Kor_RequestTypeA__c`가
  A/S(고장 신고)/B/S(캠페인·정기서비스, 고장 아님)/PDI(출고전 점검)로 나뉜다. B/S가 결측·오염의
  주 원인이므로 고장 건수 집계 시 반드시 A/S만 사용한다.
- 북미+글로벌: 고장 원인이 정형 코드(`CauseCode__c` 계통 33종, `CauseCode2__c` 현상 63종),
  `ClaimType__c`(Retailed=판매후/In Stock=판매전 등)로 구분되며 핵심 변수(고장일자·수리일자·
  사용시간)가 100% 채워져 있다.

## 현재 상태 (2026-08-14 갱신)

- 단계: 전차축(Front Axle) 파일럿 설계 진행 중. 지역 범위 확정 완료, 분자(고장 정의) 설계 중.
- **지역 범위: 북미 + 글로벌 (24,055건)로 확정.** 한국은 2차 과제로 이월.
  상세 근거는 `decisions/2026-08-14-scope-region-na-global.md` 참고.
- **컬럼**: 285개 전수 진단 완료(`notes/column-diagnosis-2026-08-14.md`). 북미+글로벌 범위에서
  한국 전용 97개 컬럼 자동 제외 → 188개로 축소. 애매한 컬럼은 아직 제거하지 않고 보류 중.
- **분자(고장 건수) 정의 진행 중**:
  - ClaimType__c: In Stock(2,374건, UsageTime 중앙값 1.5시간 확인 → 판매 전 상태, 제외 검토)
  - Status: Rejected(1,596건)가 단순 반려가 아님을 확인. Front Axle Rejected 225건 중 다수가
    실제 오일씰 누유 등 진짜 고장(승인금액 0원 71.5%는 보증만료/사용자과실 추정). Status가 아니라
    "고장 실재 여부" 기준으로 분자를 재설계해야 함. **다음 과제: 거절 사유 세부 컬럼 확인.**
- 주 분석 테이블: `Warrnaty_Claim_Status.xlsx` (북미+글로벌 24,055건).
- 엔진 계열은 엔진번호 매핑이 없어 1차 분석 범위에서 제외 (엔진개발팀 요청 시 확보 가능, 2차 예정).
  FMEA TF는 컨설턴트 일정 미정으로 보류.
- 다음 마일스톤: 거절사유 구분 완료 → 분자 정의 확정 → 분석용 컬럼 최종 선별(30~50개) → EDA 착수

## 운영 구조

- `RCI / Field` (Claude 프로젝트 방): 실제 분석 사고 전개, 의사결정
- `RCI / Method` (Claude 프로젝트 방): 방법론·통계 스터디 (WarrantyHub 블로그 비판적 활용, 현재 보류)
- 이 GitHub repo: 코드 및 의사결정 기록의 재현 가능한 저장소
- Notion (Allen's Space / FRA 프로젝트): 팀 공유용 상태 요약, 회의 기록

## 데이터 취급 규칙

- 클레임 데이터 원본(엑셀, CSV 등)은 절대 이 repo에 커밋하지 않는다 (.gitignore 참고).
- 협력사명, 특정 모델명 등 식별 정보가 필요한 예시는 익명화하거나 대표값으로 치환한다.
- 사내 정책 확인 전까지 실제 데이터를 외부 AI 도구(Claude 등)에 업로드할 때는 컬럼/샘플을 제한한다.

## 코딩 규칙

- Python, pandas 기준. 함수/스크립트에는 목적과 분모/조건화 기준을 주석으로 명시.
- 통계 검정을 쓸 때는 표본 크기와 가정(정규성, 독립성 등) 위반 가능성을 코드 주석 또는 README에 함께 기록.
